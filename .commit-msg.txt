Add destructive-command guard, fix caveman hook duplication, scope test verification to JS files

- New PreToolUse hook blocks (exit 2) irreversible git/filesystem commands and
  hook-skipping flags, unconditionally regardless of permission mode.
- caveman-activate.sh now derives its SessionStart payload from
  skills/caveman/SKILL.md instead of a hand-copied duplicate, eliminating drift
  between the two; keeps the safety-relevant sections, excludes only the
  intensity level-switching table (only relevant when /caveman is invoked
  mid-session).
- stop-verify.sh now also runs jest --findRelatedTests on changed files, and
  its file filter covers .js/.jsx in addition to .ts/.tsx.
- Removed skipDangerousModePermissionPrompt from settings.json to restore the
  confirmation step before entering bypass permission mode.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
